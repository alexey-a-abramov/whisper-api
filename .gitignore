# Dependencies
node_modules/

# Build output — rebuilt from source on publish (prepublishOnly), not committed
dist/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Env / secrets (keep the example)
.env
.env.*
!.env.example

# Tests / coverage
coverage/
.vitest/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Packaging artifacts
*.tgz

# Local whisper-api runtime state, if WHISPER_API_HOME ever points into the repo
.whisper-api/
